# frozen_string_literal: true

RSpec.describe BrandonBuilder do
  it "has a version number" do
    expect(BrandonBuilder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
